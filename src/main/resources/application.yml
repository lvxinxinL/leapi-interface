server:
  port: 8102
  servlet:
    context-path: /api
leapi:
  client:
    access-key: ghost
    secret-key: abcdefg
logging:
  level:
    org:
      springframework:
        cloud:
          gateway: trace